[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "your-package-name"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []
